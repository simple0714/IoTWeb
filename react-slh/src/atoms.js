import { atom } from 'recoil';

export const selectedAboutIdState = atom({
  key: 'selectedAboutIdState',
  default: null,
});

export const selectedPartnerIdState = atom({
  key: 'selectedPartnerId',
  default: null,
});

export const selectedProjectIdState = atom({
  key: 'selectedProjectIdState',
  default: null,
});